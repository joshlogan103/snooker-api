import express from 'express'

const router = express.Router()

// Import user Controller



// Define routes

// Get all users

router.get('/', )

// Get a user by ID

router.get('/:id', )

// Create a new user

router.post('/', )

// Update a user by ID

router.put('/:id', )

// Delete a user by ID

router.delete('/:id', )

export default router

